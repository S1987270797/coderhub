const path = require('path');
const AVATAR_PATH = path.resolve(__dirname, '../../upload/avatar');
const PICTURE_PATH = path.resolve(__dirname, '../../upload/picture');

module.exports = {
    AVATAR_PATH,
    PICTURE_PATH
}